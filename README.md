# picklebush

## memex --help

```
find regexen that would match a given text

Usage: picklebush [OPTIONS] <STEP>

Arguments:
  <STEP>  

Options:
      --debug            
  -d, --dir <DIRECTORY>  
  -h, --help             Print help
  -V, --version          Print version
```
